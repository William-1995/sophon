"""
Adapters - skill-specific integrations with framework features.

Each adapter registers its path extractors (or other hooks) with the framework.
The framework stays abstract; adapters opt-in.
"""
