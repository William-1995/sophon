"""Service layer between HTTP routes and core logic.

Subpackages are flat modules: chat (sync/async/stream), sessions, workspace,
in-process run state (SSE, cancel, HITL), OpenAI-compatible API, and helpers.
"""
