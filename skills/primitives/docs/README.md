# Docs Skill

Read Sophon docs and skill README files without falling back to shell access.

## Capabilities

- **read**: Read a Sophon doc or a skill README

## Notes

- `kind: doc` reads from `docs/`
- `kind: skill` reads a skill README (`README.md` first, then `SKILL.md`)
- `kind: list` returns available docs and skills
